package com.sanefox;

import java.sql.SQLOutput;
import java.util.*;

public class SimpleBot {
    public static void main(String[] args) {
        /*
        Scanner scanner = new Scanner(System.in);
        String firstStr = scanner.nextLine();
        String secondStr = scanner.nextLine();

        String[] firstArr = firstStr.split(" ");
        String[] secondArr = secondStr.split(" ");

        Set<String> firstSet = new HashSet<>(Arrays.asList(firstArr));
        List<String> secondSet = new ArrayList<>(Arrays.asList(secondArr));

        System.out.println(firstSet.containsAll(secondSet) && firstSet.size() == secondSet.size());

        Map<String, Integer> mapx = new HashMap<>();

         */
    }
}